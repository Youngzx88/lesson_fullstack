# ThreeJs Learn
## First Demo
- PerspectiveCamera 透视相机,有 4 个参数,
    - 第一个参数是角度（fov），也就是看的范围有多大。
    - 第二个参数是宽高比，也就是这个视椎体的宽和高的比例。
    - 第三个和第四个参数是展示视椎体的哪一部分，最近是哪，最远是哪。
- 用 Three.js 提供的轨道控制器 OrbitControls,支持通过鼠标拖动来 360 度观察 3D 场景，本质是监听鼠标事件

## dat,gui可视化调试