import { PersonIdFilterFilter } from './person-id-filter.filter';

describe('PersonIdFilterFilter', () => {
  it('should be defined', () => {
    expect(new PersonIdFilterFilter()).toBeDefined();
  });
});
